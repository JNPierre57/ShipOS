# Fixtures

All fixtures are synthetic, anonymized examples based on documented field mappings. They are NOT real Frontier logs. golden.json covers the domain expectations; filesystem fixtures for rotation, malformed lines, partial EOF and NavRoute are constructed in integration tests. Verified sources: docs/technical-validation.md.
